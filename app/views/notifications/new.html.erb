
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.9.0/jquery.min.js"></script>
<link href="http://www.bootstrapcdn.com/twitter-bootstrap/2.2.2/css/bootstrap-combined.min.css" rel="stylesheet"> 
<script src="http://www.bootstrapcdn.com/twitter-bootstrap/2.2.1/js/bootstrap.min.js"></script>


<div class="container">

  <%= form_for(:user, url:  notifications_path, html: { class: 'modal-form form-horizontal' } ) do |f| %>

        <div>
            <%= f.label :Subject%>
              
            <%= f.text_field :subject%>
        </div>
       
        <br>
        <div>
           <%= f.label :Description  %>
              
            <%= f.text_area :content %>
        </div>
       
        <br>
        <div>
            <%= f.label :Recipent %>
        </div>
        <div>
            <%= f.select(:All, options_for_select( [['Location Based', 'Location'], ['Genre Preference','Genre'],['Author Preference','Author'],['All', 'All']], selected: :All ))%>
        </div>
         <br>

         <%= f.submit "Log in" %>

        <% end %>
        
</div>


  <script type="text/javascript">
    
    // jQuery(document).ready(function() {
       $("#user_All").change(function(){
          if ($(this).val() == 'Genre')
          {
             // alert("Genre")
             $('#form-content').modal('show') 
           }
          else if ($(this).val() == 'Author')
          {
             // alert("Author")
           }
          else if ($(this).val() == 'Location')
           {
              // alert("Location")
             
           }
          else if ($(this).val() == 'All')
           {
                // alert("All")
           }
      });
// });
   </script>


  <!-- model content -->  
  <div id="form-content" class="modal hide fade in" style="display: none; ">
          <div class="modal-header">
                <a class="close" data-dismiss="modal">×</a>
                <h3>Select Location</h3>
          </div>
          <form class="contact">
             
          
            <div class="modal-body"> 
           
              <%= form_for(:rrrrrr,  url: notifications_path , html: { class: 'modal-form form-horizontal' } ) do |f| %>  
              <%#= form_for :user, :url => {:controller => "notifications", :action => "create"} do |f| %>
                 <% ReadingPreference.pluck(:genre).each do |role| %>
                  <ul>
                   <li>
                     <%= check_box_tag 'genre[]', role -%>
                     <%= h role -%>
                   </li>
                    <ul>
                 <% end %>
   
                  <div class="modal-footer">
                    <!-- <button class="btn btn-success" id="submit">Send</button> -->
                     <%= f.submit "Log in", :class=>"btn btn-success" %>
                    <a href="#" class="btn" data-dismiss="modal">Close</a>
                  </div>
             <%end%>
            </div>
           
     </form>
  </div>







 